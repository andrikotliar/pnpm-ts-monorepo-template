{
  "name": "monorepo-template",
  "scripts": {
    "dev": "pnpm run -r --parallel --aggregate-output dev",
    "build": "pnpm --filter ./apps/backend run build && pnpm --filter ./apps/frontend run build",
    "start": "node ./apps/backend/dist/server.js"
  }
}